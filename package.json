{
  "name": "mi-cv",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "preview:web": "npm run preview --workspace web",
    "pdf": "node scripts/cv-pdf.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
